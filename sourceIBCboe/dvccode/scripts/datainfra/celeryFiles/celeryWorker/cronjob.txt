* * * * * source /etc/bashrc && ans=$(/usr/bin/aws autoscaling describe-auto-scaling-instances --instance-ids `/opt/aws/bin/ec2-metadata -i | awk '{print $2}'` | grep LifecycleState | awk '{print $2}') && if [[ ("$ans" == "\"Terminating:Wait\"") || ("$ans" == "\"Terminating:Wait\",") ]] && [ ! -e /home/dvctrader/celeryWorker/killed ]; then ps aux | grep celery\ worker | awk '{print $2}' | xargs kill -SIGINT; touch /home/dvctrader/celeryWorker/killed; /bin/touch /mnt/sdf/logs/workers/`/bin/hostname`; /bin/date >> /mnt/sdf/logs/workers/`/bin/hostname`; /usr/bin/uptime >> /mnt/sdf/logs/workers/`/bin/hostname`; fi &>>/tmp/cron-log.txt
*/5 * * * *  source /etc/bashrc; mkdir -p /mnt/sdf/logs/call_run_sim_logs/`date +\%Y`/`date +\%m`/`date +\%d`; rsync -ravz `find /media/disk-ephemeral2/results-logs/ -type f -mtime -1 -print` /mnt/sdf/logs/call_run_sim_logs/`date +\%Y`/`date +\%m`/`date +\%d` &> /dev/null; 